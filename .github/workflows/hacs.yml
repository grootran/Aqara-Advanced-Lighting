# Temporarily disabled due to pending HA branding pull request
# Will be re-enabled once the branding PR is merged
name: HACS Validation

# Disabled triggers
# on:
#   push:
#     branches:
#       - main
#       - master
#   pull_request:
#   workflow_dispatch:
on: workflow_dispatch

jobs:
  hacs:
    name: HACS Action
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: HACS Action
        uses: hacs/action@main
        with:
          category: integration
